<footer>
	<div class="footer-container">
		<div class="footer-main">
			<a href="/#" class="brand-logo">
				<img src="/logo.png" alt="UniWave" />
				<div class="brand-text">
					<span class="brand-name">UniWave</span>
					<span class="brand-tagline">La comunidad universitaria</span>
				</div>
			</a>

			<nav class="footer-nav" aria-label="Footer navigation">
				<a class="footer-link" href="/#features">Funciones</a>
				<a class="footer-link" href="/#download">Descargar</a>
				<a class="footer-link" href="/#contact">Contacto</a>
				<a class="footer-link" href="/#faq">FAQ</a>
				<a class="footer-link" href="https://bandicoot.solutions" target="_blank" rel="noopener noreferrer">Bandicoot</a>
			</nav>
		</div>

		<div class="footer-bottom">
			<p>&copy; 2025 Bandicoot Solutions. Todos los derechos reservados.</p>
			<a class="footer-email" href="mailto:contact@aguslasalvia.online">
				contact@aguslasalvia.online
			</a>
		</div>
	</div>
</footer>

<style>
	footer {
		background: linear-gradient(180deg, var(--bg-color) 0%, rgba(13, 21, 38, 0.6) 100%);
		border-top: 1px solid var(--border-color);
		padding: 3.5rem 2rem 2rem;
	}

	.footer-container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.footer-main {
		display: flex;
		align-items: center;
		justify-content: space-between;
		margin-bottom: 2.5rem;
		flex-wrap: wrap;
		gap: 2rem;
	}

	.brand-logo {
		display: flex;
		align-items: center;
		gap: 0.875rem;
		text-decoration: none;
	}

	.brand-logo img {
		width: 42px;
		height: 42px;
		border-radius: 11px;
	}

	.brand-text {
		display: flex;
		flex-direction: column;
		gap: 0.1rem;
	}

	.brand-name {
		font-size: 1.2rem;
		font-weight: 700;
		color: var(--text-color);
		letter-spacing: -0.4px;
	}

	.brand-tagline {
		font-size: 0.78rem;
		color: var(--text-muted);
		font-weight: 400;
	}

	.footer-nav {
		display: flex;
		gap: 0.25rem;
		flex-wrap: wrap;
		align-items: center;
	}

	.footer-link {
		color: var(--text-muted);
		text-decoration: none;
		font-size: 0.875rem;
		font-weight: 500;
		padding: 0.45rem 0.75rem;
		border-radius: 8px;
		transition: color 0.2s ease, background 0.2s ease;
		display: block;
	}

	.footer-link:hover {
		color: var(--text-color);
		background: rgba(255, 255, 255, 0.06);
	}

	.footer-bottom {
		border-top: 1px solid var(--border-color);
		padding-top: 1.75rem;
		display: flex;
		align-items: center;
		justify-content: space-between;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	.footer-bottom p {
		color: rgba(241, 245, 249, 0.35);
		font-size: 0.82rem;
		margin: 0;
	}

	.footer-email {
		color: rgba(241, 245, 249, 0.35);
		font-size: 0.82rem;
		text-decoration: none;
		transition: color 0.2s ease;
	}

	.footer-email:hover {
		color: var(--primary-light);
	}

	@media (max-width: 768px) {
		.footer-main {
			flex-direction: column;
			align-items: center;
			text-align: center;
		}

		.footer-nav {
			justify-content: center;
		}

		.footer-bottom {
			flex-direction: column;
			align-items: center;
			text-align: center;
			gap: 0.5rem;
		}
	}

	@media (max-width: 480px) {
		footer {
			padding: 3rem 1.5rem 1.75rem;
		}
	}
</style>
